import { defineConfig } from 'astro/config';

export default defineConfig({
  // Indirizzo pubblico del sito (passerà a https://italiannextgen.it quando i DNS Aruba saranno attivi)
  site: 'https://tiri6.github.io',
});
